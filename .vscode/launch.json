{
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Launch Program",
            "target": "${workspaceFolder}/build/${command:cmake.buildType}/Rsa100",
            "cwd": "${workspaceFolder}/build/${command:cmake.buildType}",
            "valuesFormatting": "parseText"
        }
    ]
}