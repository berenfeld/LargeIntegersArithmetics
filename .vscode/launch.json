{
    "configurations": [
    {
        "type": "gdb",
        "request": "launch",
        "name": "Launch Program",
        "target": "./build/${command:cmake.buildType}/TestLargeInts",
        "cwd": "${workspaceRoot}",
        "valuesFormatting": "parseText"
    }
    ]
}